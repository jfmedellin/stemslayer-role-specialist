# Corpus synthesis and the shared measurements need nothing but numpy.
numpy==2.4.1
soundfile==0.13.1

# Training. Install PyTorch from the matching CUDA index first, because the
# default wheel is CPU-only and this trains on a local NVIDIA GPU:
#   python -m pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cu130
demucs==4.1.0
